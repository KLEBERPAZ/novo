@app.route('/escola_1')
def escola_1():
    if 'username' not in session:
        return redirect(url_for('login'))
    ano = session.get('ano', 'N/A')  # Obtém o ano da sessão ou usa 'N/A' como valor padrão
    return render_template('escola_1.html', ano=ano)
